# 控制清单 · 性能与可访问性硬约束（Control Manifest）

| 字段 | 值 |
| --- | --- |
| **项目** | ASHEN STEP《灰烬之步》 |
| **文档版本** | **v0.3**（Sprint 3 · S3-B：新增 §8 音频硬约束 A-01~A-05，收口 OOS-2 编号归属冲突 F-1，裁决为 **Option A · 并轨**） |
| **作者** | 程基岩（工程负责人） |
| **上游依据** | `design/art/art-bible.md` **v0.3** §2.1/§2.2 主色板与授权明度变体、§3 光照/体积、§8.2 状态编码、§9.1 色盲友好、§9.2 对比度；`design/art/hud-a11y-signature.md` **v1.1**（D7 签字稿）；`design/assets/sprint2-asset-spec.md` §1.1/§5.1（FLAG-2/FLAG-3 裁决）；`design/concept/game-concept.md` 风险5；**`design/audio/s3b-save-load-audio-spec.md` v1.0 §1.7（AUD-A1~A5）与 §1.4（AUD-V6）；`docs/architecture/adr/adr-005-audio-bus-architecture-and-world-mode-presets.md`（Draft）** |
| **下游衔接** | 技术美术（着色器/VFX/布光）、UI（设置菜单/可访问性开关）、玩法程序（守卫/声环/时间）、**音频（总线布局/世界预设/cue 电平）**、CI 断言脚本 |
| **性质** | **硬约束清单**：任何系统/资产/预设超出下列上限，须在评审标注「性能漂移 / 可访问性漂移」并由主理人裁决；CI 可选静态断言（见 §7）。 |

---

## 1. 渲染 / 光照硬约束（来自美术 §3）

| 编号 | 约束 | 上限 / 取值 | 说明 |
| --- | --- | --- | --- |
| R-01 | 主渲染器 | **Forward+（Vulkan）** | 低配回退 Compatibility（GL），关体雾/动态GI |
| R-02 | 动态点光同屏 | **MVP ≤12 / Tier2 ≤32** | Forward+ 聚类；超 32 触发断言 |
| R-03 | 投影光数量 | **≤1（月光 DirectionalLight3D）** | 守卫提灯 OmniLight 默认 `shadow=false`；高端预设可 +最近 1–2 提灯 |
| R-04 | 体积雾密度（base） | **≤ 0.05** | 冷调染色 `#10141C`/`#3E5C76`；禁暖雾主导 |
| R-05 | 熄灯过场雾 ramp | **≤ 0.12 且持续时间 ≤ 0.4s** 后回落 | 美术 §3.3「黑暗吞没」 |
| R-06 | 静态几何 GI | **LightmapGI 烘焙**（每区域一次） | 动态物体不进烘焙 |
| R-07 | 动态光构成 | 仅 可互光源 + 守卫提灯 + 少量彩窗 SpotLight | 静态大光池走烘焙，非实时 |
| R-08 | 尘埃加法粒子 | 每光柱上限 + **全局 additive 上限**（建议 ≤ 2000） | 仅光柱/点光内可见；CI 断言 |
| R-09 | 目标帧率 | **60fps @ 1080p**（GTX1060 档） | 画质预设 低/中/高 |

---

## 2. 时间 / 慢动作硬约束（来自美术 §9.3 + 风险5）

| 编号 | 约束 | 取值 | 说明 |
| --- | --- | --- | --- |
| T-01 | 时间缩放可调范围 | **0.1× – 1.0×** | 用户可调 |
| T-02 | 凝神默认缩放 | **0.25×**（区间 0.1–0.3） | 下限 0.1，防物理不稳 |
| T-03 | 完全冻结 | 用显式暂停（`paused` / `time_scale=0`），**非玩法常态** | 仅菜单/设置 |
| T-04 | 粒子时间同步 | 凝神时同步 `GPUParticles3D/CPUParticles3D.time_scale` | 否则尘埃/声环不与世界同速（ADR-003） |

---

## 3. 眩晕 / 光敏硬约束（来自美术 §9.3）

| 编号 | 约束 | 取值 | 说明 |
| --- | --- | --- | --- |
| V-01 | 频闪禁限 | **禁止 > 3Hz 亮灭** | 光敏防护 |
| V-02 | 暴露脉动频率 | **≤ 2Hz**，幅度温和 | 封顶（美术 §9.3「封顶」） |
| V-03 | 屏震 | **默认关、可开、幅度可调** | 暴露提示**不强制**屏震 |
| V-04 | 体积雾选项 | 提供「减弱雾 / 关闭雾」 | 眩晕防护 |
| V-05 | 动态模糊 | **默认关、可关** | 减少眩晕 |
| V-06 | 转场 | 缓动（ease），**禁硬切闪光** | — |

---

## 4. 对比度 / 色盲硬约束（来自美术 §2.3 / §9.1 / §9.2）

| 编号 | 约束 | 取值 | 说明 |
| --- | --- | --- | --- |
| C-01 | UI 正文/数值对比度 | **≥ 4.5:1**（WCAG AA） | — |
| C-02 | 关键指示对比度（可疑度/暴露） | **≥ 7:1** | — |
| C-03 | 世界可读要素亮度差（锥缘/落点预演） | **≥ 3:1** | 边界清晰 |
| C-04 | 光池 vs 阴影判断 | **靠亮度边界，不靠冷/暖色** | 色盲安全 |
| C-05 | 机制信息编码 | **亮度 + 形状 + 图标 三重**，不依赖单一色相 | 红绿/绿红/蓝黄三类 100% 可读 |
| C-06 | 色盲模式开关 | 警报语义色 `#D64545` → **`#F0C070` 高亮 + 图标** | 不出主色板新色相（`#F0C070` 为 `#C8862F` 同琥珀色相族明度变体，色相 37.5° vs 34.1°）。**旧口径「→`#C8862F`」作废**：警戒本就是 `#C8862F`，映射后两档亮度比塌缩至 1.00:1（art-bible v0.3 §9.1 修正） |
| C-07 | 危险提示 | 警报语义色 `#D64545` **绝不单独使用**（必配形状/脉动/图标）；`#7A2E2E` **仅**作 α≤0.35 低不透明底纹 | 美术 §2.3/§8.2。`#7A2E2E` 对深色面板 1.91:1（理论极值 2.26:1），**数学上永不达 C-02/C-03**，不得作文字/边框/图标；其语义角色由 `#D64545` 承接（D7 签字稿 §2.3/§3） |

> **v0.2 同步说明（C-06/C-07）**：本次仅把语义色**取值**对齐 D7 裁决与 art-bible v0.3，**阈值（C-01 4.5:1 / C-02 7:1 / C-03 3:1）与编号语义一律不变**，§7 CI 断言维持 **WARN-ONLY 不阻断构建**。
> - HUD 落地权威色板见 `design/art/hud-a11y-signature.md` v1.1 §4；工程常量落 `src/ui/hud_colors.gd`。
> - C-06 的色盲区分**不只靠色值**：脉冲频率（警戒 0.5Hz 单拍 / 警报 2.0Hz 双拍，均在 V-02 ≤2Hz 内）与图标面积编码（空心圆环 `?` / 实心三角 `!`）**常驻生效、不随开关切换**（art-bible v0.3 §9.1）。
> - 信息边界色 `#4E6E8A`（3.21:1 @`#1B1B1F`）为 `#3E5C76` 同色相明度变体，用于「须被感知的 UI 组件边界」；装饰性描边仍用 `#3E5C76`（免于 C-03，art-bible v0.3 §8.1 二分规则）。

---

## 5. 文本 / 字幕硬约束（来自美术 §9.3）

| 编号 | 约束 | 取值 | 说明 |
| --- | --- | --- | --- |
| X-01 | UI 文本缩放 | **100% – 150%** 不破版 | — |
| X-02 | 字幕 | 关键音景（足音类型/诱饵落点/守卫警示语）带**说话者标识 + 音景图标** | — |

---

## 6. 玩法侧预算硬约束（来自风险5 / architecture §4）

| 编号 | 约束 | 取值 | 说明 |
| --- | --- | --- | --- |
| G-01 | 同区活动守卫 | **MVP ≤ 8 / Tier2 ≤ 16** | 锥 10Hz 错峰 + 网格 |
| G-02 | 同屏声环 VFX | **≤ 8** | Tween/shader 自毁 |
| G-03 | 锥体重算频率 | **≤ 10Hz/守卫**（错峰） | 事件置脏 + 节流，非逐帧 |
| G-04 | 可疑度 FSM 决策 | **≤ 10Hz** | 状态机节流 |
| G-05 | A\* 寻路 | **仅状态转换触发**，路径缓存 | 非逐帧 |

---

## 7. CI / 构建期静态断言（建议）

- 编辑器导入/构建脚本校验：场景中实时 `OmniLight3D`/`SpotLight3D` 同屏 **> 32** → 警告（R-02）。
- 体积雾 `volumetric_fog_density` **> 0.05** base 或 ramp **> 0.12 / > 0.4s** → 警告（R-04/R-05）。
- 静态网格缺 UV2 → 警告（ADR-004 LightmapGI 前置）。
- 同屏声环实例 **> 8** → 警告（G-02）。
- 暴露脉动 shader 频率参数 **> 2Hz** → 警告（V-02）。

> 任何告警不阻断构建（lean 评审），但须在评审记录「漂移」并获主理人裁决。

---

## 8. 音频硬约束（来自音频规格 §1.7 / ADR-005）

> **本节的存在理由（F-1 · Option A 并轨裁决）**
> 本清单 v0.2 的六族 30 条**无一条覆盖音频** —— 不是遗漏，是当时项目里根本没有音频代码。S3-B 落地了第一批（`src/core/audio_director.gd` + `arts/audio/audio_bus_layout.tres`），随之出现一个编号治理问题：`design/art/accessibility-matrix.md` §2 表前注自述「所有约束编号严格对应 control-manifest.md，无新增未定义约束」，而它的音频四行（行 19–22）只能引 `AUD-*`。
> 两条出路里选了 **A（并轨）**：**编号权威源只能有一个**，否则下一个人要同时记住两套前缀，才能判断「这条约束到底归谁管」。选 B（写一段豁免声明）等于把这个负担永久留在文档里。
> **取值不在这里重新定义**，一律引用 `design/audio/s3b-save-load-audio-spec.md` v1.0 §1.7。本节是**编号与归口**，规格是**内容**；两者冲突以规格为准，并回来修本节。

| 编号 | 约束 | 取值 | 说明 |
| --- | --- | --- | --- |
| A-01 | 承载信息的 UI 音声像 | **单声道居中**，且单声道下混后信息不丢失 | = AUD-A1。单侧听力损失玩家；居中同时是「系统在说话」的空间语义。属**资产**属性，由资产验收把关，运行时无对应断言 |
| A-02 | 音频**永不作为唯一通道** | 每条承载信息的音必有**字幕 + 视觉**孪生 | = AUD-A2，继承 C-05 / X-02。S3-B 六个存档/读档事件全部满足；读档成功的听觉回归与遮罩**同帧读同一个 `world_fade_alpha()` 返回值**，不是两条独立曲线 |
| A-03 | 失败 / 拒绝音响度上限 | **绝不高于对应的成功音**（音频规格 §3 硬点 1） | = AUD-A3。不用音量制造惊吓。工程实现见 `AudioDirector.effective_level_db()`（纯函数，故意不含重触发阶梯，好让单次电平可被无时钟断言） |
| A-04 | 周期性振幅调制上限 | **禁止 > 3 Hz** | = AUD-A4，**V-01「禁 >3Hz 亮灭」的听觉对偶**。重复触发须走音频规格 §2.5 重触发阶梯（`AudioDirector` 常量 `UI_RETRIGGER_SUPPRESS_MS = 80` / `UI_REPEAT_FLOOR_DB = -9`），不得靠调用方自律 |
| A-05 | 分路音量 + 「UI 音」开关 | Master / Music / Ambience / SFX_World / SFX_UI **五路独立** + UI 音总开关，进设置 | = AUD-A5。**已实现（`src/ui/audio_settings_panel.gd` + `.tscn`）**：三条滑杆（Master / World / SFX_UI，百分比→dB 按**振幅线性** `linear_to_db(p/100)`，夹在 [-40, +6] dB）+「UI 音效」开关，经 `SaveManager.save_prefs("audio", …)` 存盘、`_ready()` 回填。★ **门控范围仅 UI 反馈音**：`AudioDirector.ui_sound_enabled` 只在 `play_cue()` 首行拦截，**不触及**世界总线、读档淡入与世界模式预设；关闭后**字幕与视觉通道照常**（A-02 兜底，已在 `test_audio_cues.gd` 反向断言）。⚠ **未竟**：Music / Ambience / SFX_World 三路尚未各自暴露滑杆（当前由父线 World 统管），总线树已就绪、面板为数据驱动（`BUS_ROWS`），补行即可 |

**架构锚点（非新增编号）**：总线树与三个世界预设的取值在 `docs/architecture/adr/adr-005-…md`（D-1~D-8）。`AUD-V6`（模式切换 120 ms 缓动、单帧增益变化 ≤3 dB）是 **V-06「转场缓动，禁硬切」的听觉对偶**，归 V-06 管辖，不另编号。

**机器可查 vs 评审可查**（诚实登记，避免把「有编号」误当成「有防线」）：

| 编号 | 谁在守 | 强度 |
| --- | --- | --- |
| A-01 | 资产验收（人工） | 评审 |
| A-02 | 人工 + `test_save_ui.gd` **两条**路径已断言「音 + 孪生通道」同时到场：存档成功（cue + 「系统」字幕）、动作被拒（cue + `denial_hint()`）。其余四个事件目前只有人工核对 | 部分机器可查 |
| A-03 | 人工（`effective_level_db()` 已做成纯函数，断言随时可加） | 评审 |
| A-04 | 人工（阶梯已实现且可 `set_retrigger_ladder_enabled(false)` 关闭以做确定性断言） | 评审 |
| A-05 | `tests/unit/test_audio_bus_layout.gd` 守其前置的五路总线树与路由；`tests/unit/test_audio_settings_panel.gd` 守百分比→dB 映射、单路互不串扰、开关直达 `AudioDirector`、prefs 往返与「不误伤邻近 a11y 段」；`tests/unit/test_audio_cues.gd` 守★ 关 UI 音后字幕/视觉不失效（均**硬失败**，非 §7 的 WARN-ONLY） | 机器可查 |

> **与 §7 的区别**：§7 是构建期**警告**，不阻断。§8 的机器可查项走 GUT 单元测试，**会红、会阻断**。这是刻意的：总线路由错了没有任何听觉症状（游戏照样出声，只是出错了地方），只有断言能抓。
