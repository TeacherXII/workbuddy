# 可访问性三档分级与特性矩阵（Accessibility Tiering & Feature Matrix）
**《灰烬之步》ASHEN STEP · 美术 / 视觉侧可访问性落地补充**

| 字段 | 值 |
| --- | --- |
| **项目** | ASHEN STEP《灰烬之步》 |
| **引擎 / 平台** | Godot 4.4 / PC·Steam（键鼠 + 手柄） |
| **评审强度** | lean |
| **文档版本** | **v0.4**（Sprint 3 · S3-B OOS-2 音频 a11y 收口：新增行 19–22 + 编号治理注）｜前版 v0.3（Sprint 3 · O-1 焦点环裁决同步）｜v0.2（Sprint 2 告警色治理同步） |
| **作者** | 林绘澄（Art Director / 美术与视觉表现指导） |
| **上游依据** | `design/art/art-bible.md` **v0.4** §2.1 主调色板 / §8.2 关键指示 / §9.1 色盲友好（**告警色权威源**）/ **§9.4.1 焦点环独立语义槽**；`docs/architecture/control-manifest.md`（C-01~C-07、X-01/X-02、V-01~V-06、T-01/T-02、R-04/R-05）；`design/gdd/systems/core-hud-a11y.md` 八节；`design/concept/game-concept.md` §5 Tier2；design/audio/s3b-save-load-audio-spec.md **v1.0** §1.7 音频可访问性基线（AUD-A1~A5）；docs/architecture/adr/adr-005-audio-bus-architecture-and-world-mode-presets.md（Draft） |
| **下游衔接** | 美术圣经 §9（设计规范）、`core-hud-a11y` 设置菜单与 `A11ySettings`、技术美术（眩晕/对比度参数）、CI 静态断言（control-manifest §7） |
| **性质** | **分级启用表（Tier Enablement Table）**：本文与美术圣经 §9 互为补充——§9 是「设计规范」（全量硬基线），本文是「分级启用表」（哪些特性在哪一档默认开 / 可选 / 增强）。本文**不修改** §9 任何取值，仅在 §9 之上显式引入 Basic 合规底线档。 |

> **四支柱一致性红线（硬约束，全程适用）**
> - 肃穆压迫不被闪亮/高饱和 UI 破坏；可访问性开关与辅助指示的视觉处理须**低调**（见 §4）。
> - Sensation 走「质地」而非「烟花」；辅助反馈是物理、可触的，不是庆祝式闪光。
> - 本文所有可访问性视觉（辅助落点、自定义主题、可达焦点）不得引入主色板外新色相（对齐美术 §2.4）。

---

## 0. 术语与图例

**启用状态图例（用于矩阵各档单元格）**
| 标记 | 含义 |
| --- | --- |
| **强制(开)** | 硬约束，三档均生效、不可关闭（由 control-manifest / 美术 §9 锁死） |
| **默认开** | 出厂默认启用，用户可关 |
| **默认关(可开)** | 出厂默认关闭，用户可开启 |
| **可选/增强层** | 仅 Comprehensive 提供的增强，复用既有约束，不新增硬约束编号 |
| **可用未默认** | 能力存在但出厂不自动启用（编码已保障安全，开关为增强） |

**覆盖障碍类型缩写**
`低视力`=低视力/老花/强光环境 · `色盲`=色觉缺陷(红绿/绿红/蓝黄) · `听障`=听觉障碍/聋/噪声环境 · `眩晕`=眩晕/前庭 · `光敏`=光敏性癫痫 · `运动`=运动/肢体受限 · `认知`=认知负荷/神经多样

**代价等级**：极低（设计期/配置，无运行时重负） · 低（少量 UI/着色器切换） · 中（需输入系统或主题系统工作量，部分逻辑交程基岩）

---

## 1. 三档定义（定位与启用范围）

### 1.1 Basic（合规底线 / 最低保障）
- **定位**：保证游戏**可玩**、满足基础无障碍底线。出厂即强制生效的一组硬约束，玩家无需进入设置即可受益。
- **启用范围**：对比度硬达标、亮度/边界编码、色盲三重编码（全局默认）、频闪/脉动/转场防护、屏震/动态模糊**默认关**、雾密度硬封顶、字幕默认开、键盘/手柄可达、时间缩放默认 0.25×（粗步进）。
- **覆盖**：最核心的视觉 / 听觉 / 运动障碍群体。
- **出厂默认档**：否（Basic 是「底线」，不是默认体验档；游戏出厂默认设为 **Standard**）。

### 1.2 Standard（完整可访问性 / 默认推荐）
- **定位**：美术圣经 §9 原「Standard→Comprehensive」的主体，即项目 Tier2 显式可访问性要求。覆盖大多数障碍玩家，是**出厂默认档**。
- **启用范围**：在 Basic 之上完整开启——色盲模式开关（C-06）、时间缩放完整 0.1–1.0× 细步进（T-01）、雾选项 FULL/REDUCED/OFF（V-04）、屏震可开可调（V-03）、动态模糊可关（V-05）、文本 100–150% 缩放（X-01）、完整字幕带说话者标识+音景图标（X-02）、核心动作可重映射。
- **覆盖**：绝大多数障碍玩家（低视力 / 色盲 / 听障 / 眩晕 / 运动）。

### 1.3 Comprehensive（超越标准 / 重度障碍友好）
- **定位**：在 Standard 之上为重度障碍 / 多重障碍玩家提供的增强层。所有增强**复用既有约束编号**，不新设硬约束。
- **启用范围**：在 Standard 之上增加——自定义配色主题（过 C-01/C-02，不越 §2.4）、全输入重映射、辅助落足/路径提示（复用 §8.2 预演）、时间缩放增强（细步进+锁定+显式暂停读场，不破 T-01/T-03）、降低认知负荷选项、音频线索额外可视化增强。
- **覆盖**：重度 / 多重障碍玩家（含认知与神经多样）。

> **与美术 §9 的关系**：§9 全量基线 = 本文 Standard 档要求；§9 之上列出的 Comprehensive 增强 = 本文 §2 的 Comprehensive 行；本文新增的 Basic 档 = §9 中「强制类硬约束」的显式兜底（保证即便用户不碰设置也达标）。**无任何取值被修改**，无漂移。

---

## 2. 特性矩阵（核心交付）

> 说明：矩阵行级列出「覆盖障碍类型」与「关联约束编号」（三档共享）；各档单元格给出「启用状态 · 实现与性能代价」。所有约束编号严格对应 `control-manifest.md`，无新增未定义约束。

| # | 可访问性特性 | 覆盖障碍 | Basic（状态 · 代价） | Standard（状态 · 代价） | Comprehensive（状态 · 代价） | 关联约束 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 对比度达标（UI/关键/世界） | 低视力 | **强制(开)** UI≥4.5:1、关键≥7:1、世界≥3:1 · 极低（配色纪律+CI断言） | **强制(开)** + 全界面（含二级文本/菜单）审计覆盖 · 低 | **强制(开)** + 可选高对比增强主题 · 低 | C-01 / C-02 / C-03 |
| 2 | 亮度 / 边界编码（光池 vs 阴影、世界要素） | 色盲·低视力 | **强制(开)** 靠亮度边界不靠色相；世界要素≥3:1 · 极低 | **强制(开)** · 极低 | **强制(开)** + 边界可加粗/提亮选项 · 低 | C-04 / C-03 |
| 3 | 色盲三重编码（亮度+形状+图标） | 色盲 | **强制(开)** 不依赖单一色相；警报语义色 **`#D64545`** 必配形状/脉动/图标（`#7A2E2E` 仅作 α≤0.35 底纹，**非语义载体**，1.91:1 数学上永不达 C-03）· 极低 | **强制(开)** · 极低 | **强制(开)** + 图标库可扩展 · 低 | C-05 / C-07 |
| 4 | 色盲模式开关（暴露色替换） | 色盲 | **可用未默认**（编码已安全，开关为增强）· 低（着色器/图标叠加） | **默认关(可开)** 警报语义色 **`#D64545` → `#F0C070`**（vs 警戒 `#C8862F` 亮度比 **1.81:1**；vs `#16181D` **10.55:1**）+ 实心三角 `!` 图标 · 低 | **默认关(可开)** + 与自定义主题联动 · 低 | C-06 |
| 5 | 频闪 / 脉动防护（光敏） | 光敏·眩晕 | **强制(开)** 禁 >3Hz 频闪；暴露脉动 ≤2Hz 温和 · 极低（着色器约束） | **强制(开)** · 极低 | **强制(开)** + 脉动幅度可进一步减弱 · 低 | V-01 / V-02 |
| 6 | 转场缓动（禁硬切闪光） | 光敏·眩晕 | **强制(开)** ease 转场 · 极低 | **强制(开)** · 极低 | **强制(开)** · 极低 | V-06 |
| 7 | 眩晕防护 · 体积雾选项 | 眩晕·光敏 | 雾密度硬封顶（R-04 ≤0.05、R-05 ramp ≤0.12/≤0.4s）保障，**无 OFF 选项** · 极低 | **默认开** FULL/REDUCED/OFF 三档 · 低（开关；关雾略降氛围，须美术把控 §1 红线） | **默认开** + 关雾时世界要素靠边界/图标补强可读性 · 低 | V-04（base R-04 / R-05） |
| 8 | 眩晕防护 · 屏震 | 眩晕 | **默认关(可开)** 暴露不强制屏震 · 极低 | **默认关(可开)** 幅度可调 · 极低 | **默认关(可开)** + 可完全关闭所有相机位移 · 极低 | V-03 |
| 9 | 眩晕防护 · 动态模糊 | 眩晕 | **默认关** · 极低（关模糊省性能） | **默认关(可开)** 完整开关 · 极低 | **默认关(可开)** · 极低 | V-05 |
| 10 | 时间缩放 | 运动·认知·低视力 | 默认 0.25×（T-02），粗步进滑杆（区间内）· 极低（`time_scale`） | **默认开** 完整 0.1–1.0× 细步进（T-01）· 极低 | **默认开** + 凝神可长期锁定 + 显式暂停读场辅助（T-03）；**不突破 T-01 上限 1.0× / 下限 0.1×** · 低 | T-01 / T-02（Comprehensive + T-03） |
| 11 | 文本缩放 | 低视力 | 固定 100%（不破版）· 极低 | **默认开** 100%–150% 不破版（X-01）· 低（布局重排） | **默认开** + 字幕/UI 分离缩放档（仍限 X-01 范围）· 低 | X-01 |
| 12 | 字幕 | 听障 | **默认开** 核心音景字幕 + 说话者标识 · 低 | **默认开** 完整（说话者标识 + 音景图标，X-02）· 低 | **默认开** + 字幕字号独立更大档（±X-01）· 低 | X-02 |
| 13 | 输入可达（键盘 / 手柄） | 运动 | **强制(开)** 焦点环 **`#F0C070`** 2px 实线、**0Hz 不脉冲**（10.20:1，过 C-02）+ 位置 + 背景提亮，不靠颜色 alone；键盘/手柄可达 · 低 | **强制(开)** · 低 | **强制(开)** + 可达焦点提示增强 · 低 | 美术 **§9.4.1**（**独立语义槽，非 C-06**）/ C-02 |
| 14 | 输入重映射（全映射） | 运动 | 仅预设键位可达（无重映射）· — | 核心动作可重映射 · 中（输入系统，**逻辑交程基岩**） | **可选/增强层** 全输入重映射（键鼠/手柄全映射+切换）· 中 | 美术 §9.4（逻辑交程基岩） |
| 15 | 音频提示可视化增强 | 听障 | 无（仅世界内声环，非专属） | 无专属（世界内声环已存在，§8.2） | **可选/增强层** 音景类型图标 + 方向指示 + 危险音环境可视化；复用 X-02 音景图标，**无新增约束** · 中 | X-02 / §8.2 |
| 16 | 辅助落足 / 路径提示 | 认知·低视力·运动 | 核心玩法已有下一步预演（Tier1，世界内足迹残影）· —（核心非专属 a11y） | 同核心预演（§8.2） | **可选/增强层** 安全落点高亮 + 路径提示线，复用预演系统（`#C8862F` 极低微光，不喧哗）· 低 | §8.2 / 系统① |
| 17 | 自定义配色主题 | 低视力·色盲·光敏 | 无 | 无（仅色盲模式预设替换） | **可选/增强层** UI/HUD 明度与强调色可调；须过 C-01/C-02，**不得引入主色板外新色相**（对齐 §2.4）· 中 | C-01 / C-02 / C-06 |
| 18 | 降低认知负荷 | 认知 | 无（核心已极简，§8.1） | 无专属 | **可选/增强层** 信息密度降档 + 关键提示优先级 + 世界内要素降噪；复用 §8.1 极简哲学 · 低 | §8.1 / §8.4 |
| 19 | 音频提示视觉同源（每条承载信息的音必有视觉孪生） | 听障·认知 | **强制(开)** 音频**永不作为唯一通道**：S3-B 全部 6 个存档/读档事件均配字幕（X-02，说话者「系统」）+ 视觉；读档成功的听觉回归与 `_fade` 遮罩**同帧读同一个 `world_fade_alpha()` 返回值**，非两条独立曲线 · 极低（规格纪律，零运行时成本） | **强制(开)** + 全量 UI 音事件逐条覆盖审计 · 极低 | **强制(开)** + 与行 15 音景图标联动（图标是**冗余**不是替代）· 低 | X-02 / C-05 / **AUD-A2**（音频域，见表后注） |
| 20 | 分路音量独立可调 + UI 音开关 | 听障·认知 | **可用未默认** 持久化通道已存在（`SaveManager.save_prefs("audio", …)`，`test_save_manager.gd` L448 已在测），但**无设置 UI** · 低 | **默认开** Master / Music / Ambience / SFX_World / SFX_UI **五路独立**滑杆 + 「UI 音」总开关；★ 关闭 UI 音后字幕与视觉通道**不得**随之失效（由行 19 兜底）· 中（设置 UI + 持久化，**逻辑交程基岩**） | **默认开** + 单声道下混开关（**AUD-A1**）+ 失败/拒绝音额外衰减档（听觉敏感）· 中 | **AUD-A5 / AUD-A1 / AUD-A3**（音频域） |
| 21 | 凝神 / 暂停期音频不变调 | 眩晕·光敏·认知 | **强制(开)** 三个世界预设（FLOWING / FOCUS / PAUSED）**均不动 `pitch_scale`**（ADR-003 原决策）；模式切换 **120 ms 缓动**、单帧增益变化 ≤3 dB（V-06 的听觉对偶）；**禁止 >3 Hz 周期性振幅调制**（V-01 的听觉对偶）· 极低（bus 参数纪律） | **强制(开)** · 极低 | **强制(开)** + 世界 ducking 深度可调（**不得**借此解除「不变调」铁律）· 低 | ADR-003 / **ADR-005** / V-01·V-06 的听觉对偶 **AUD-A4 / AUD-V6** |
| 22 | 状态区分不依赖音频通道（双向不塌缩） | 听障·色盲·认知 | **强制(开)** C-05 三重编码（亮度+形状+图标）**三维全部为视觉** —— 色盲档**不得**引入「靠听声区分状态」作为补偿；反向亦然：静音 / 听障玩家不因缺声丢失任何状态信息 · 极低（编码纪律） | **强制(开)** · 极低 | **强制(开)** + 行 15 音景可视化作为**冗余通道**，不得成为唯一载体 · 中 | C-05 / C-06 / C-07 / X-02 / **AUD-A2** |

> **⚠️ 行 3 / 行 4 的关键补充 —— 告警色治理三件套常驻生效（art-bible v0.3 §9.1）**
>
> | 维度 | 警戒 CAUTION | 警报 ALARM | 是否随色盲开关切换 |
> | --- | --- | --- | --- |
> | **色值（默认模式）** | `#C8862F` | `#D64545` | — （亮度比仅 **1.44:1**，故须下两维兜底） |
> | **色值（色盲模式，C-06）** | `#C8862F`（不变） | **`#F0C070`** | **是**（仅此一维随开关） |
> | **脉冲频率** | **0.5Hz 单拍呼吸** | **2.0Hz 双拍** | **否 · 常驻**（均在 V-02 ≤2Hz 内，不违 V-01） |
> | **图标面积编码** | **空心圆环** + `?` | **实心三角** + `!` | **否 · 常驻**（面积级差异，小尺寸下远优于笔画级） |
>
> **为何频率/面积编码常驻**：默认模式下 `#C8862F` vs `#D64545` 亮度比仅 1.44:1 —— 红绿色盲玩家**在未开色盲模式时**就已难以区分。若把这两维绑定在开关上，等于要求玩家「先知道自己需要它」才能获得它。**常驻是更诚实的默认**，也使行 4 的 Basic 档「可用未默认」在编码上真正安全。

> **⚠️ 行 19–22 的编号治理 —— `AUD-*` 不是 control-manifest 编号**
>
> ★ **已裁决（Option A）**：主理人确认由程基岩在 `control-manifest.md` 新增 §8「音频硬约束」`A-01~A-05`（取值引用音频规格 §1.7）。本文行 19–22 **暂引 `AUD-*`**，待 §8 落地后 **renumber 为 `A-0N`**、§3.3 覆盖表同步增 5 行。编号体系最终保持单一权威源。
>
> `docs/architecture/control-manifest.md` v0.2 的六族约束（R-01~09 / T-01~04 / V-01~06 / C-01~07 / X-01~02 / G-01~05，共 30 条）**无一条覆盖音频**，§7 的 5 条 CI 断言同样不含音频。因此行 19–22 引用的 `AUD-*` 编号，其**权威源是 `design/audio/s3b-save-load-audio-spec.md` v1.0 §1.7**（AUD-A1~A5）与 §1.4（AUD-V6），**不属于 control-manifest**。
>
> 这意味着本文 §2 表前注「所有约束编号严格对应 `control-manifest.md`，无新增未定义约束」**对行 19–22 暂不成立**。两条出路，待主理人裁决：
> - **(A · 推荐)** 程基岩在 `control-manifest.md` 新增 §8「音频硬约束」，编号 **A-01~A-05**，取值直接引用音频规格 §1.7；本文行 19–22 改引 `A-0N`，§3.3 覆盖表增 5 行。**编号体系保持单一权威源。**
> - **(B · 最小改动)** 行 19–22 直接引 `AUD-*`，并保留本注块作为显式豁免声明。代价是本文从此有两套编号来源。
>
> 在裁决落地前，行 19–22 的**约束内容全部有效**（它们是可执行的规格），仅**编号归属**待定。

---

## 3. 与现有文档的一致性结论

### 3.1 与美术圣经 §9 的一致性（对齐 art-bible **v0.4**）

> **v0.2 修订说明**：本节 v0.1 原声称「与 §9 **逐项数值一致**」。该声明在 art-bible 升版 v0.3（D7 `#D64545` 同步 + `#4E6E8A` 授权 + 告警色治理）后**一度为假** —— 本文行 3/行 4/§4 仍持旧 C-06 口径（`#7A2E2E` 作语义色、色盲模式映射至 `#C8862F`）。v0.2 已将上述各处校正，下表为**逐项对照的实际取值**，不再以概括性断言代替核对。

| 项 | art-bible v0.3 实际取值 | 本文对应位置 | 一致 |
| --- | --- | --- | --- |
| 对比度阈值 | C-01 ≥4.5:1 · C-02 ≥7:1 · C-03 ≥3:1（§9.2） | 行 1 | ✅ |
| 警报 ALARM 语义色（默认） | **`#D64545`**（§2.2 / §8.2） | 行 3 · §4 | ✅ v0.2 已校正 |
| 警报 ALARM 语义色（色盲 C-06） | **`#F0C070`**（§9.1，vs `#C8862F` 1.81:1） | 行 4 | ✅ v0.2 已校正 |
| `#7A2E2E` 血锈定位 | 仅 α≤0.35 底纹 + 世界紧急光 + 血迹锈斑（§2.1）；**不得**作边框/图标/文字 | 行 3 · §4 | ✅ v0.2 已校正 |
| 警戒 CAUTION 语义色 | `#C8862F`（§2.1，两模式均不变） | 行 3 注 · §4 | ✅ |
| 信息边界 / Slate Rise | **`#4E6E8A`**（§2.1 / §8.1 描边二分规则） | §4 | ✅ v0.2 已补 |
| 脉冲频率编码 | 警戒 0.5Hz 单拍 / 警报 2.0Hz 双拍，**常驻**（§9.1） | 行 3/4 注 | ✅ v0.2 已补 |
| 图标面积编码 | 空心圆环 `?` / 实心三角 `!`，**常驻**（§9.1） | 行 3/4 注 | ✅ v0.2 已补 |
| 时间缩放 | 0.1–1.0×（§9.3） | 行 10 | ✅ |
| 文本缩放 / 字幕 | 100–150% · 说话者标识 + 音景图标（§9.3） | 行 11 / 行 12 | ✅ |
| 焦点环 FOCUS（**独立语义槽**） | **`#F0C070`** 2px 实线 · **0Hz 不脉冲** · 10.20:1 过 C-02（§9.4.1） | 行 13 · §4 | ✅ **v0.3 已校正**（原 `#C8862F` 仅 5.65:1，不过 C-02） |

- 本文新增 Basic 档 = §9 中「强制类硬约束」的显式兜底，**未修改 §9 任何基线**。
- 本文 Comprehensive 增强 = §9 之上的增量，全部复用 §9 / control-manifest 既有编号。
- **口径纪律**：art-bible §2/§8/§9 为告警色**唯一权威源**。本文任何色值若与其不符，以 art-bible 为准并须回改本文，不得反向推翻。

### 3.2 与 core-hud-a11y GDD 的一致性（无漂移）
- GDD §7 列出的约束集合 `C-01~C-07、X-01/X-02、V-01~V-06、T-01/T-02` 在本文矩阵**全部出现**（见 §3.3 覆盖核对），且档位默认状态与 GDD §3 `A11ySettings` 字段默认一致（屏震默认关 V-03、动态模糊默认关 V-05、雾 FULL/REDUCED/OFF V-04、文本缩放默认 1.0 X-01、字幕默认开 X-02、时间缩放默认 0.25 T-02）。
- GDD §8 Tier2（完整可访问性包） = 本文 Standard 档；本文 Basic/Comprehensive 为其下/上延伸，未改变 GDD 范围纪律。

### 3.3 约束编号覆盖核对（证明：全覆盖、无遗漏、无新增）
| 约束编号 | 出现于矩阵行 | 所在档位 | 备注 |
| --- | --- | --- | --- |
| C-01 | 行 1 | 三档强制 | UI 对比度 |
| C-02 | 行 1 | 三档强制 | 关键指示对比度 |
| C-03 | 行 1 / 行 2 | 三档强制 | 世界亮度差 |
| C-04 | 行 2 | 三档强制 | 光池 vs 阴影靠亮度 |
| C-05 | 行 3 | 三档强制 | 三重编码 |
| C-06 | 行 4 | Standard/Comprehensive | 色盲模式开关 |
| C-07 | 行 3 | 三档强制 | 危险提示不单色 |
| X-01 | 行 11 | Standard/Comprehensive | 文本缩放 |
| X-02 | 行 12 / 行 15 | Standard/Comprehensive | 字幕 / 音频可视化基础 |
| V-01 | 行 5 | 三档强制 | 频闪禁限 |
| V-02 | 行 5 | 三档强制 | 脉动频率 |
| V-03 | 行 8 | 三档（默认关） | 屏震 |
| V-04 | 行 7 | Standard/Comprehensive | 雾选项（base R-04/R-05） |
| V-05 | 行 9 | 三档（默认关） | 动态模糊 |
| V-06 | 行 6 | 三档强制 | 转场缓动 |
| T-01 | 行 10 | Standard/Comprehensive | 时间缩放范围 |
| T-02 | 行 10 | 三档 | 凝神默认 0.25× |
| R-04 / R-05 | 行 7（基础硬封顶） | 三档强制 | 雾密度上限（眩晕防护底座） |
| 美术 §9.4 | 行 13 / 行 14 | 三档强制 / 增强 | 输入可达 / 重映射 |
| §8.1 / §8.2 / §8.4 | 行 16 / 行 18 | 核心 / 增强 | 预演 / 认知负荷 |
| AUD-A1 | 行 20 | Comprehensive | 单声道居中 / 下混不丢信息（**音频域编号**，见 §2 表后注） |
| AUD-A2 | 行 19 / 行 22 | 三档强制 | 音频永不作为唯一通道（**音频域编号**） |
| AUD-A4 / AUD-V6 | 行 21 | 三档强制 | 禁 >3 Hz 振幅调制 / 模式切换 120 ms 缓动（V-01 / V-06 的听觉对偶，**音频域编号**） |
| AUD-A5 / AUD-A3 | 行 20 | Standard / Comprehensive | 分路音量与 UI 音开关 / 失败音不制造惊吓（**音频域编号**） |

> **结论**：control-manifest 全部 18 条可访问性硬约束（C-01~07、X-01/02、V-01~06、T-01/02）+ 雾底座（R-04/R-05）均在矩阵中显式出现；行 14/15/16/17/18 及行 10 的 Comprehensive 增强**全部复用既有约束编号**。**例外：行 19–22（音频侧）引用 `AUD-*` 音频域编号** —— control-manifest 现无任何音频约束，编号归属待裁决（见 §2 表后注）；这四行的约束**内容**已由 `design/audio/s3b-save-load-audio-spec.md` v1.0 定义并可执行，仅**编号体系**待并轨。

### 3.4 一处需主理人知晓的研判（非冲突，已自洽）
- 主理人意图中 Comprehensive 含「更高时间缩放上限」。但 control-manifest **T-01 硬锁可调范围 0.1–1.0×**、**T-03 锁完全冻结仅限显式暂停**。故本文将 Comprehensive 时间缩放实现为「T-01 范围内更细步进 + 凝神长期锁定 + 结合 T-03 的显式暂停读场辅助」，**明确不突破 T-01 上限 1.0×（实时速度）与下限 0.1×（物理稳定）**。这是对主理人意图在硬约束下的安全落地，不引入漂移。

---

## 4. 视觉纪律与四支柱一致性（可访问性开关的低调处理）

- **开关本体低调**：设置菜单内的可访问性开关、辅助指示（辅助落点高亮、自定义主题 UI）沿用面板基调——半透明暗面板 `#1B1B1F`@70–85% + `#3E5C76` 细描边，**无外发光、无霓虹、无圆角糖果**（美术 §8.1）。
- **辅助落点提示**：Comprehensive 的落点高亮复用 §8.2 下一步预演语言，用 `#C8862F` **极低强度微光 + 足迹残影**，不喧哗、不破坏肃穆压迫。
- **自定义主题边界**：Comprehensive 自定义配色**仅允许在明度 / 强调色明度维度调整**，UI 强调色取自 `#C8862F`（警戒）/ **`#D64545`**（警报）的明度变体，且每次切换须过 C-01/C-02 自检；**禁止引入主色板外新色相**（对齐 art-bible §2.2 规则 4）。**已授权的同色相明度变体仅三个**：`#D64545`（血锈系）· `#4E6E8A`（阴影蓝系）· `#F0C070`（烛琥珀系，承载**色盲档警报**与**焦点环**两个互不竞争的语义槽，art-bible v0.4 §2.2 规则 4 / §9.4.1；**琥珀族至此封顶，不得再有第三个语义征用**）。**`#7A2E2E` 不得作强调色来源** —— 其 1.91:1（理论极值 2.26:1）数学上永不达 C-03，仅可作 α≤0.35 底纹。
- **信息边界用色（art-bible v0.3 §8.1 描边二分规则）**：可访问性 UI 中「玩家若看不见就会丢失信息或操作目标」的边界（存档槽、列表分隔、可聚焦行）须用 **`#4E6E8A`**（3.21:1 @`#1B1B1F`，过 C-03）；纯装饰性结构描边才用 `#3E5C76`（2.53:1，免于 C-03）。行 1 Comprehensive「高对比增强主题」的边界提亮即取 `#4E6E8A`。
- **可达焦点环（v0.3 校正 · O-1 Option A）**：键盘/手柄焦点环用 **`#F0C070` 2px 实线描边 + 恒 0Hz 不脉冲**（对 `#1B1B1F` = **10.20:1**，过 **C-02**），配**位置**（跟随焦点）与**背景提亮档**构成三重编码，不靠颜色 alone；与美术 **§9.4.1** 一致。
  > **两点须同时理解**：① **取值**——焦点环是「关键操作指示」，须过 C-02（≥7:1）；原 `#C8862F` 仅 **5.65:1**，过 C-01 但**不过 C-02**，故合规驳回。② **归属**——焦点环归 **美术 §9.4.1 独立语义槽**，**不归 C-06**；C-06 管的是色盲模式下警报语义色 `#D64545`→`#F0C070` 的替换。此前个别文档写「焦点环 `#C8862F`**(C-06)**」，**括注本身是错的**，已随本次一并更正。
  > **撞色缓解（规范级，非实现细节）**：色盲模式下焦点环与警报徽标同为 `#F0C070`，靠 **频率维（0Hz vs 2.0Hz）+ 形状维（空心矩形描边 vs 实心三角）+ 锚定维（面板 chrome vs 世界锚）** 三重正交分离，C-05 不塌缩。**焦点环一旦脉冲，该缓解即失效**（美术 §9.4.1 硬性禁令）。
- **眩晕防护的美术代价**：关闭体积雾（V-04 OFF）会略微削弱「空气凝重」氛围，属预期权衡；美术侧确保关闭雾时世界要素仍靠边界/图标保可读（矩阵行 7 Comprehensive），不破 §1 肃穆压迫红线。

---

## 5. 下游衔接与落地建议

- **美术圣经 §9（现 v0.4）**：保留为「设计规范」（全量硬基线，**告警色唯一权威源**）；本文为「分级启用表」，二者并列引用。**引用时须锁版本**：本文 v0.3 对齐的是 art-bible **v0.4**（`#D64545` 默认警报 / `#F0C070` 色盲告警 **+ 焦点环** / `#4E6E8A` 信息边界 / **焦点 FOCUS 为独立语义槽 §9.4.1**）。art-bible 若再升版，本文行 3 / 行 4 / **行 13** / §3.1 对照表 / §4 须同步复核，**不得沿用「已自洽」的历史断言**。
- **core-hud-a11y GDD §3 `A11ySettings`**：建议按档分组标注默认值——Basic 强制项不可出现在开关里；Standard 项为出厂默认；Comprehensive 项为「进阶」分区（自定义主题 / 全重映射 / 辅助落点 / 认知负荷 / 音频可视化）。
- **技术美术**：行 7/8/9/10 的眩晕与时间参数已进 control-manifest（R-04/R-05、V-03/V-04/V-05、T-01/T-02），按现值实现；Comprehensive 增强（行 10 锁定/读场、行 16 落点高亮）为增量，须在不突破硬约束前提下实现。
- **CI 静态断言**（control-manifest §7）：已覆盖全部硬约束（R-02/R-04/R-05/G-02/V-02 等）；三档分级不改变断言阈值，仅改变「默认开关状态」，断言继续有效。
- **待回填 / 不归本档**：全输入重映射（行 14）的逻辑实现交程基岩；自定义主题的主题系统工作量由 UI/前端评估。
- **音频侧（新增，v0.4）**：行 20 的五路音量滑杆与「UI 音」开关属设置 UI + 持久化，**逻辑交程基岩**（持久化通道 `SaveManager.save_prefs("audio", …)` 已存在，只差 UI）；行 19/21/22 为规格纪律，由音频指导在 design/audio/ 维护，本文只做分级启用登记，**不复制取值**。

---

*可访问性三档分级与特性矩阵 v0.3 完成。本文与美术圣经 **v0.4** §2/§8/§9 逐项对照见 §3.1 表（v0.2 已校正 v0.1 遗留的旧 C-06 口径；v0.3 已同步 O-1 焦点环裁决）；所有 Comprehensive 增强复用既有约束编号，未新增硬约束。任何后续系统若偏离本矩阵档位默认，须在评审标注「可访问性漂移」并由主理人裁决。*

**v0.4 变更记录（Sprint 3 · S3-B OOS-2 音频 a11y 收口）**
| # | 位置 | 变更 | 依据 |
| --- | --- | --- | --- |
| 1 | 矩阵表 | 新增行 19–22（视觉同源 / 分路音量 / 不变调 / 不依赖音频区分）| 音频规格 v1.0 §1.7、ADR-003、ADR-005(Draft) |
| 2 | §2 表后 | 新增「AUD-* 编号治理」注块 | control-manifest 无音频约束（v0.2 全文核对）|
| 3 | §3.3 | 覆盖表增 4 行；**结论句改写**（原「未新增任何约束编号」已为假）| 同上 |
| 4 | §5 | 新增音频侧下游衔接 bullet | 行 20 逻辑归属程基岩 |

**v0.3 变更记录（Sprint 3 · O-1 焦点环裁决 · 林绘澄签字）**
| # | 位置 | 变更 | 依据 |
| --- | --- | --- | --- |
| 1 | 行 13 | 焦点环 `#C8862F` → **`#F0C070`** 2px 实线 + **0Hz 不脉冲**；关联约束由「美术 §9.4 / 概念」改为「美术 **§9.4.1**（独立语义槽，**非 C-06**）/ **C-02**」 | art-bible **v0.4** §9.4.1（O-1 Option A） |
| 2 | §3.1 对照表 | 「焦点环」行更新为 `#F0C070` / 10.20:1 / 过 C-02，并标注为**独立语义槽** | 同上 |
| 3 | §4「可达焦点环」 | 补全取值改判理由（C-02 合规驳回 `#C8862F` 的 5.65:1）+ **归属澄清**（非 C-06）+ **撞色缓解为规范级**（0Hz / 空心 / 面板锚三维正交） | 同上 |
| 4 | §4「自定义主题边界」 | `#F0C070` 用途由「仅色盲模式」扩为「色盲档警报 + 焦点环」两槽；**琥珀族封顶** | art-bible v0.4 §2.2 规则 4 |

> **★ 严禁全局批量替换 `#C8862F`**：本次**只改焦点环**这一个槽位。`#C8862F` 作为**警戒 CAUTION**（行 3/4 注 · §3.1 · §4）与**暖光/光池/落足微光/辅助落点**（行 16 · §4）的取值**完全现行有效，一处不改**。

**v0.2 变更记录（FLAG-C 闭环 · 主理人授权）**
| # | 位置 | 变更 | 依据 |
| --- | --- | --- | --- |
| 1 | 行 3 | 色盲三重编码的语义色 `#7A2E2E` → **`#D64545`**；`#7A2E2E` 显式降格为「α≤0.35 底纹，非语义载体」 | art-bible v0.3 §2.1 / §2.3 |
| 2 | 行 4 | C-06 色盲映射 `#7A2E2E`→`#C8862F` **改为** `#D64545`→**`#F0C070`**（1.81:1，原 1.00:1 亮度塌缩）+ 实心三角图标 | art-bible v0.3 §9.1（FLAG-2 完整修正） |
| 3 | §2 表后 | 新增「告警色治理三件套常驻生效」注（色值/频率/面积三维 + 常驻理由） | art-bible v0.3 §9.1 |
| 4 | §3.1 | **删除已为假的「与 §9 逐项数值一致」概括性断言**，改为逐项对照表并标注各项实际取值与校正状态；增列「口径纪律」 | FLAG-C |
| 5 | §4 | 自定义主题强调色来源 `#7A2E2E` → **`#D64545`**；列明三个授权明度变体；新增 `#4E6E8A` 信息边界条目（描边二分规则） | art-bible v0.3 §2.2 / §8.1 |
| 6 | §5 / 页脚 | 自洽声明改为**锁版本引用** art-bible v0.3 实际值，并要求升版时强制复核 | FLAG-C |

> **美术域内已全部对齐**（本次闭环）：`art-bible.md` v0.3 · 本文 v0.2 · `hud-a11y-signature.md` v1.1 · `asset-manifest.md` · `entity-inventory.md` · `sprint2-asset-spec.md`。
>
> **✅ 域外已同步（无需再跟）**：`docs/architecture/control-manifest.md` **C-06/C-07 已于 v0.2 同步**（程基岩，取值对齐 `#F0C070` / `#D64545`，阈值与编号语义不变）。
>
> **⏳ 域外仍持旧口径（不在本次授权范围，须责任人同步）**：
> | 文件 | 位置 | 旧口径 | 责任人 |
> | --- | --- | --- | --- |
> | `src/ui/hud_colors.gd` | L36 | `HUD_COLOR_ALARM_CB := HUD_COLOR_CAUTION`（=`#C8862F`，塌缩 1.00:1） | 程基岩（Batch C） |
> | `production/sprints/batchc-impl-spec.md` | L146 / **L167** | L167 **明文论证**「SUSPICIOUS 与 ALERT 同为琥珀…C-05/C-07 仍成立」—— 即 FLAG-2 已推翻的旧理据 | 程基岩 |
> | ~~`design/ux/ux-spec.md`~~ | §ALERT L227 | ~~色盲模式→`#C8862F` 高亮+图标~~ | **✅ 已同步**（Sprint 3，林绘澄）→ `#F0C070` |
> | ~~`design/gdd/systems/core-hud-a11y.md`~~ | L62 / L111 | ~~暴露 `#7A2E2E`→`#C8862F`~~ | **✅ 已同步**（Sprint 3，林绘澄）→ 警报 `#D64545`→`#F0C070` |
> | ~~`design/gdd/systems/patrol-ai.md`~~ | L71 | ~~危险 `#7A2E2E` 必配图标；色盲→`#C8862F`~~ | **✅ 已同步**（Sprint 3，林绘澄）→ `#D64545` 必配图标；色盲→`#F0C070` |
> | ~~`production/sprints/batchc-impl-spec.md`~~ | L26 / L167 / L835 | ~~D7 摘要 `CB = Caution`；L167「SUSPICIOUS 与 ALERT 同为琥珀」旧理据；L835 `ALARM_CB = #C8862F`~~ | **✅ 已同步**（Sprint 3，林绘澄）→ `#F0C070` |
> | `design/gdd/sprint2-story-candidates.md` | L80 | 色盲 `#7A2E2E→#C8862F` | ⏳ **文策渊**（不在 Sprint 3 授权范围） |
> | `production/epics/E09-core-hud-a11y.md` L58/L66/L100 · `E08-patrol-ai.md` L123 | 见各 AC | 色盲 `#7A2E2E→#C8862F`+图标 | ⏳ **主理人 / 程基岩**（不在 Sprint 3 授权范围） |
>
> **Sprint 3 焦点环同步（O-1 Option A，全部 ✅ 本轮完成）**：`art-bible.md` §2.2/§9.4.1 · 本文行13/§3.1/§4 · `hud-a11y-signature.md` §4.1/§5.2 · `sprint2-asset-spec.md` §4.2/§4.7/§5.1.1 · `asset-manifest.md` 行14 · `core-hud-a11y.md` L66/L108 · `ux-spec.md` A14/§3.3 · `E11-save-manager.md` L104 · `sprint2-stories.md` L290。`save-system.md` §7 由文策渊同步 ✅。
> **⏳ 仍待程基岩**：`src/ui/hud_colors.gd` 新增 `HUD_COLOR_FOCUS := Color("#F0C070")`（**新增常量，不改既有值**）+ `HUD_COLOR_ALARM_CB` 改 `#F0C070`（Batch C 遗留）。
>
> **历史评审记录不改**（`design/reviews/sprint0/1/2-*.md`、`production/sprints/sprint1-stories.md`）：属已冻结的时点快照，按治理惯例保留原文，不追溯改写。
