# HUD 三档色值签字稿 · E09 Core HUD + 可访问性（a11y）
**《灰烬之步》ASHEN STEP · 美术 / 视觉侧 · HUD alert-state 三档权威色值**

| 字段 | 值 |
| --- | --- |
| **项目** | ASHEN STEP《灰烬之步》 |
| **引擎 / 平台** | Godot 4.4 / PC·Steam（键鼠 + 手柄） |
| **关联交付** | E09 Core HUD（Sprint 2，Tier2 a11y 完整包） |
| **文档版本** | v1.0（签字稿 · 解决评审 D7 待裁决项） |
| **作者 / 签字人** | **林绘澄（Art Director / 美术与视觉表现指导）** |
| **上游依据** | `design/art/art-bible.md` §2/§8/§9（视觉身份九节）· `design/gdd/systems/core-hud-a11y.md` §6（C-01/C-02/C-03）· `design/art/accessibility-matrix.md`（三档矩阵）· `design/ux/ux-spec.md` §HUD/ALERT · Batch C 设计就绪评审（文策渊发现的 D7 项） |
| **性质** | **美术签字稿（Art Sign-off）**：本文件给出的色值即 E09 a11y 的**权威 HUD 三档色板**；工程实现须以此为准，不得另行改色（见 §7 签字条款）。 |

> **D7 裁决摘要**：设计策划文策渊在 Batch C 设计就绪评审中发现——HUD 警示红 `#7A2E2E` 压在深色面板（`#16181D`）上对比度仅 **1.91:1**（对纯黑理论极值 **2.26:1**），数学上不可能达到项目可访问性红线 **C-02（关键载体文字 ≥7:1，WCAG AAA 口径）**。本签字稿将该色改判为「**可读载体**」策略：用近白载体色 `#DCE3EC`（13.74:1）作为信息传递主色，红/琥珀仅作**非文字语义提示**（边框、脉冲、图标），不承载可读性关键文字。本稿即 D7 的裁决落地点。

---

## 0. 签字结论（Verdict）

> **结论：PASS（美术签字确认）**
>
> 三档 HUD 色板已全部按 WCAG 2.1 算法核算，关键载体文字以 `#DCE3EC` 承载，稳过 C-02；警戒/警报的语义红/琥珀经提亮或重定位后过 C-03（≥3:1 UI 组件边界）。D7 待裁决项关闭。
>
> **签字后必须同步的两项（非色值本身问题，属下游落地）**：
> 1. **下游文档改色**：`design/ux/ux-spec.md`（§ALERT：194/227）、`design/assets/entity-inventory.md`（#22：44）、`design/assets/asset-manifest.md`（#暴露 ALERT：251）目前仍把 `#7A2E2E` 直接写作「暴露 ALERT 的边框/脉冲/图标色」。须改为：边框/脉冲/图标用 `#D64545`，`#7A2E2E` 仅保留作**低不透明填充**（压在白色载体文字之下）。
> 2. **平静态描边确认**：平静态中性描边 `#3E5C76` 在深色面板上 **2.53:1**，低于 C-03（≥3:1）。它属于装饰性结构描边（信息由白色文字承载），不属 C-01/C-02/C-03 规制的信息载体边界；若后续它须作为「可感知的 UI 组件边界」出现，须提亮至 ≥3:1 的中性板岩明度变体（交美术圣经 §8.1 复核，不在本 alert-tier 范围）。

---

## 1. 计算方法与基线

- **算法**：WCAG 2.1 对比度。sRGB 相对亮度 `L = 0.2126·R + 0.7152·G + 0.0722·B`（各通道先经 `c/12.92`（≤0.03928）或 `((c+0.055)/1.055)^2.4` 线性化）。对比度 `= (L_light + 0.05) / (L_dark + 0.05)`，结果四舍五入至两位小数。
- **参考面板（深色）**：以 `#16181D` 为**权威核算基准**（用户给定「`#16181D` / `#1C1F26` 一类暗色」）。同时交叉核算 `#1C1F26`、`#1B1B1F`（美术圣经 §2.1 / GDD §2 面板基调），确认所有载体/强调色在最暗基准上都达标，故可安全用于三者任一。
- **阈值口径**（对齐 `core-hud-a11y.md` §6，并采用用户给定的 C-03 广义 WCAG 2.1 读法）：
  - **C-02**：关键指示（可疑度 / 暴露）载体文字 **≥7:1**（WCAG AAA）。
  - **C-01**：UI 正文 / 数值 **≥4.5:1**（WCAG AA）。
  - **C-03**：大字号（≥18pt 或 ≥14pt 粗）**或 UI 组件边界 / 图形对象** **≥3:1**。*说明*：GDD §6 原文把 C-03 限在世界可读要素（锥缘/落点），本签字稿对 HUD alert-state 采用 C-03 的广义读法——任何作为「UI 组件边界/图标」的红/琥珀都必须 ≥3:1。这正是 `#7A2E2E` 连作边框都不合格的根因。

---

## 2. HUD alert-state 三档色值签字表

> 主表对比度基于权威基准面板 **`#16181D`**。每档「可读载体」统一为 `#DCE3EC`（见 §3）。

### 2.1 通用可读载体（三档共用）

| 角色 | HEX | 用途 | vs `#16181D` | C-02(≥7) | C-01(≥4.5) | C-03(≥3) |
| --- | --- | --- | --- | --- | --- | --- |
| **可读载体 Carrier** | `#DCE3EC` | 所有承载信息的文字 / 数值 / 数字 / 图标填充（可疑度数字、暴露标签、状态文字） | **13.74:1** | ✅ PASS | ✅ PASS | ✅ PASS |

> 交叉核对：vs `#1C1F26` = 12.76:1；vs `#1B1B1F` = 13.28:1。三档面板均稳过 C-02。

### 2.2 三档 alert-state

| 档 | 状态 | 语义色 HEX | HUD 元素 / 状态 | vs `#16181D` | C-02(≥7) | C-01(≥4.5) | C-03(≥3) | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **Tier 1 平静 CALM** | 守卫未察觉 / 可疑度≈0 | `#3E5C76`（Shadow Blue） | 面板描边、结构分隔、idle 状态（**非信息载体**，信息由白字承载） | 2.53:1 | —（非文字） | —（非文字） | ⚠️ 2.53 < 3（装饰描边，见 §0 待办 2） | 与美术圣经 §8.1 面板基调一致；仅为装饰性结构色 |
| **Tier 2 警戒 CAUTION** | 守卫起疑 / 可疑度中段 | `#C8862F`（Candle Amber） | 上升中的可疑度条**边框** + `?` 图标 + 温和脉冲（≤2Hz，V-02） | **5.84:1** | —（非文字） | ✅ PASS | ✅ PASS | 即品牌「烛琥珀」；清晰橙调，与警报红区分 |
| **Tier 3 警报 ALARM** | 暴露 / `exposure_detected` | `#D64545`（提亮血锈变体） | 警报**边框** + `!` 图标 + 脉冲（≤2Hz，V-02）+（可关）屏震（V-03） | **4.06:1** | —（非文字） | ⚠️ 4.06 < 4.5（仅作边框/图标，C-03 已达标） | ✅ PASS | 血锈 `#7A2E2E` 的**明度变体**（同红相，提亮），非新色相（对齐美术 §2.4） |

### 2.3 品牌深红 `#7A2E2E` 的定位（关键裁决）

| HEX | 用途 | vs `#16181D` | vs 纯黑(极值) | 结论 |
| --- | --- | --- | --- | --- |
| `#7A2E2E`（Blood Rust） | **仅**作低不透明填充（压在白色载体文字之下）/ 3D 世界紧急光（`art-bible.md` §3.2）/ 血迹锈斑 | 1.91:1 | **2.26:1** | **三项全 Fail**（C-02/C-01/C-03 均不达标）。**不得**用作任何 HUD 文字、关键条载体或 C-03 UI 边框/图标。 |

> 计算验证：`#7A2E2E` 相对亮度 ≈ 0.0629；即便对纯黑（L=0）其对比度上限也仅为 `(0.0629+0.05)/0.05 = 2.26:1`，故「2.26:1」为其理论极值——数学上**永远不可能**达到 C-02(≥7:1) 或 C-03(≥3:1)。这就是 D7 必须改判为「可读载体」策略的根本原因。

---

## 3. 可读载体（Readable Carrier）策略

**原则**：HUD 上一切「承载可读性关键信息」的像素，统一用近白载体色 `#DCE3EC`（13.74:1，过 C-02）。红 / 琥珀**只作非文字语义提示**，绝不承载可读性关键文字或关键条主体。

**必须用 `#DCE3EC` 承载的信息（强制）**：
- 可疑度数值（眼 / ? / ! 旁的数字，C-02）。
- 暴露 ALERT 标签文字、`exposure_detected` 屏角提示的文案（C-02）。
- 道具 / charges 数值、凝神状态文字、所有 HUD 正文与菜单文字（C-01）。
- 图标**填充**：? / ! / 眼 等状态图标以 `#DCE3EC` 描边或填充，保证形状可读（C-05/C-07 三重编码之「形状」）。

**红 / 琥珀仅作（非文字语义）**：
- **边框（border）**：警报红 `#D64545` 描边、警戒琥珀 `#C8862F` 描边——仅作「状态分界线」提示，且须 ≥3:1（C-03，已核算通过）。
- **脉冲（pulse）**：暴露脉动 ≤2Hz（V-02）、幅度温和（V-01/V-03 防护）；脉动是「亮度 + 形状」提示，不靠颜色 alone。
- **图标（icon）**：! / 危险符以 `#DCE3EC` 填充 + 红/琥珀描边，三重编码（C-07 绝不单色）。
- **低不透明填充**：`#7A2E2E` 仅以低 alpha（建议 ≤0.35）作屏角/守卫处血锈**底纹**，其上是白色载体文字——可读性由白字保证，红底仅为氛围（§1 肃穆压迫，不破红线）。

**禁止**：
- 禁止用 `#7A2E2E` 写任何 HUD 文字、数值、关键条主体。
- 禁止用 `#7A2E2E` 作 C-03 边界/图标（1.91:1 不达标）；其语义角色由 `#D64545` 承接。
- 禁止引入主色板外新色相（美术 §2.4）；`#D64545` 是 `#7A2E2E` 的明度变体，合规。

---

## 4. 完整 HUD 三档色板 + GDScript 常量建议

### 4.1 色板总表

| 名称 | HEX | 归一化 RGB (0–1) | 档位 / 角色 | 对比度(vs `#16181D`) | 阈值 |
| --- | --- | --- | --- | --- | --- |
| Carrier 近白载体 | `#DCE3EC` | (0.863, 0.890, 0.925) | 三档共用·所有可读信息 | 13.74:1 | C-02 ✅ |
| Calm 中性板岩蓝 | `#3E5C76` | (0.243, 0.361, 0.463) | Tier1·装饰性结构描边 | 2.53:1 | 装饰（见 §0） |
| Caution 烛琥珀 | `#C8862F` | (0.784, 0.525, 0.184) | Tier2·警戒边框/图标/脉冲 | 5.84:1 | C-01/C-03 ✅ |
| Alarm 提亮血锈 | `#D64545` | (0.839, 0.271, 0.271) | Tier3·警报边框/图标/脉冲 | 4.06:1 | C-03 ✅ |
| Alarm-Fill 品牌深红 | `#7A2E2E` | (0.478, 0.180, 0.180) | Tier3·**仅低不透明填充**/世界紧急光 | 1.91:1 | 仅填充，非载体 |
| Panel 深色面板(基准) | `#16181D` | (0.086, 0.094, 0.114) | HUD 面板底（对比度核算基准） | — | — |

> 备选警报红（同红相族，预批准）：`#E0584F` (4.80:1，连 C-01 也过) —— 若后续审计要求警报边框自身也达 C-01(≥4.5:1)，可换用此更亮变体；二者均属本签字稿授权色，工程不得另造。

### 4.2 GDScript 常量（Godot 4.4，供工程写入；建议置于 `hud_colors.gd` 或 `Theme` 常量块）

```gdscript
# ============================================================================
# HUD a11y 三档色板 —— 林绘澄美术签字稿 v1.0（design/art/hud-a11y-signature.md）
# 权威色值，工程不得另改。Godot 4.4 用 Color.from_string() 解析十六进制。
# 归一化 RGB 已附于注释，便于直接 Color(r, g, b) 或 Theme 覆盖。
# ============================================================================

# 参考深色面板（对比度核算基准；实际 HUD 面板取 #16181D / #1B1B1F / #1C1F26 其一）
const HUD_COLOR_PANEL_BASE  := Color.from_string("#16181D", Color.BLACK)   # (0.086, 0.094, 0.114)

# 通用可读载体（三档共用）—— 所有承载信息的文字/数值/数字/图标填充
const HUD_COLOR_CARRIER     := Color.from_string("#DCE3EC", Color.WHITE)   # (0.863, 0.890, 0.925)  C-02 13.74:1

# Tier 1 平静 —— 装饰性结构描边（非信息载体）
const HUD_COLOR_CALM        := Color.from_string("#3E5C76", Color.WHITE)   # (0.243, 0.361, 0.463)  装饰，见签字稿 §0

# Tier 2 警戒 —— 上升可疑度条边框 / ? 图标 / 温和脉冲
const HUD_COLOR_CAUTION     := Color.from_string("#C8862F", Color.WHITE)   # (0.784, 0.525, 0.184)  C-01/C-03 5.84:1

# Tier 3 警报 —— 警报边框 / ! 图标 / 脉冲（血锈 #7A2E2E 的明度变体，非新色相）
const HUD_COLOR_ALARM       := Color.from_string("#D64545", Color.WHITE)   # (0.839, 0.271, 0.271)  C-03 4.06:1

# 品牌深红 —— 仅低不透明填充（alpha ≤ 0.35，压在白色载体文字下）/ 3D 世界紧急光
# 禁止作为 HUD 文字、关键条载体或 C-03 边界/图标（1.91:1 不达标）
const HUD_COLOR_ALARM_FILL  := Color.from_string("#7A2E2E", Color.WHITE)   # (0.478, 0.180, 0.180)  仅填充

# 色盲模式（C-06）：警报语义色替换为烛琥珀高亮 + 图标，三重编码
const HUD_COLOR_ALARM_CB    := HUD_COLOR_CAUTION                           # 色盲模式下 Alarm 边框/图标用 #C8862F
```

---

## 5. 色盲模式映射（C-06 / C-07）

- **默认模式**：Tier3 警报 = 白载体文字(`#DCE3EC`) + 红边框/图标(`#D64545`) + `!` 图标 + ≤2Hz 脉冲。三重编码（亮度 + 形状 + 位置/图标），绝不单色（C-07）。
- **色盲模式开启（C-06）**：警报语义色 `#D64545` → 替换为 `#C8862F`（烛琥珀高亮）+ 图标（C-06）。仍三重编码，不靠单一色相。
- **Tier2 警戒** 在两种模式下均用 `#C8862F`（本就是琥珀），靠 `?` 图标 + 脉冲区分于警报。
- **禁止**在色盲模式下用纯红/纯琥珀单独表意——必须配形状/图标/位置（C-05/C-07）。

---

## 6. 与现有文档的一致性 & 待同步项

**已自洽（无漂移）**：
- 本稿阈值（C-01/C-02/C-03）与 `core-hud-a11y.md` §6、`art-bible.md` §9.2、`accessibility-matrix.md` §2 行1 完全一致。
- 警戒琥珀 `#C8862F` = 美术圣经 §2.1 品牌「烛琥珀」，未引入新色相（对齐 §2.4）。
- 警报红 `#D64545` = 品牌「血锈 `#7A2E2E`」的明度变体（同红相），符合 `accessibility-matrix.md` §4「强调色仍取自 `#7A2E2E` 的明度变体」之规定，**非新色相**。
- 可读载体 `#DCE3EC` 与文策渊评审结论一致。

**待同步（下游文档须按本签字稿改色，属落实动作，不改色值本身）**：
| 文档 | 位置 | 现状 | 应改为 |
| --- | --- | --- | --- |
| `design/ux/ux-spec.md` | § ALERT（L194 / L227） | 「屏角脉冲 + 守卫处血锈 `#7A2E2E`」「`#7A2E2E` + 脉动 + 图标」 | 边框/脉冲/图标 → `#D64545`；`#7A2E2E` 仅作低不透明填充（白字之上） |
| `design/assets/entity-inventory.md` | #22 暴露 ALERT（L44） | 「`#7A2E2E` + 脉动 + 图标」 | 语义色 → `#D64545`；`#7A2E2E` 标注为「仅填充」 |
| `design/assets/asset-manifest.md` | #暴露 ALERT（L251） | 「`#7A2E2E` + 脉动 + 图标」 | 语义色 → `#D64545`；`#7A2E2E` 标注为「仅填充」 |
| `design/gdd/systems/core-hud-a11y.md` | §5 反馈（L50） | 「暴露 `#7A2E2E`+脉动+图标」 | 同步为 `#D64545` 语义 + `#7A2E2E` 仅填充 |

> 上述同步不改变任何数值基线或约束编号，仅把「暴露 ALERT 的边框/脉冲/图标色」从不合格的 `#7A2E2E` 校正为本签字稿授权的 `#D64545`，消除 D7 落地残留。

---

## 7. 美术签字（Art Sign-off）

**本人，林绘澄（Art Director / 美术与视觉表现指导），就 E09 Core HUD + 可访问性（a11y）的 HUD alert-state 三档色值，作出如下美术签字：**

1. 本文件（`design/art/hud-a11y-signature.md` v1.0）给出的色板——可读载体 `#DCE3EC`、平静态 `#3E5C76`、警戒 `#C8862F`、警报 `#D64545`、品牌深红 `#7A2E2E`（仅填充）——即为 E09 a11y 的**权威 HUD 三档色值**。
2. 所有对比度数值均按 WCAG 2.1（sRGB 相对亮度）核算，并以最暗面板 `#16181D` 为保守基准；在 `#1B1B1F` / `#1C1F26` 上同样达标。
3. **工程实现须以此为准，不得另行改色、不得用 `#7A2E2E` 承载 HUD 文字或作 C-03 边界/图标。** 若需 C-01 余量更大的警报边框，仅可改用本稿预批准的 `#E0584F`，不得另造色值。
4. 任何后续 HUD/主题实现若偏离本签字稿色值，须在评审标注「视觉漂移 / a11y 漂移」并由主理人裁决。

> **签字结论：PASS（D7 关闭）**
> 签字人：林绘澄（Art Director）
> 交付版本：design/art/hud-a11y-signature.md · v1.0

---

*本签字稿与美术圣经 §2/§8/§9、core-hud-a11y GDD §6、accessibility-matrix 三档矩阵、ux-spec 全量自洽，无数值漂移；仅校正下游文档中残留的「`#7A2E2E` 直接作暴露 ALERT 边框」误用，落实现有 D7 裁决。*
